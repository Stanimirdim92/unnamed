<?php
$children = $this->viewModel()->getCurrent()->getChildren();
$view = $children[0];
?>
<!doctype html>
<html lang="en" class="no-js">
    <head>
        <meta charset="utf-8">
        <title><?=$view->webTitle?></title>
        <meta name="robots" content="noindex, nofollow">
        <meta name="author" content="Stanimir Dimitrov - stanimirdim92 at gmail dot com">
        <meta name="HandheldFriendly" content="True">
        <meta name="MobileOptimized" content="320">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="apple-mobile-web-app-capable" content="yes">
        <meta name="mobile-web-app-capable" content="yes">
        <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
        <meta http-equiv="cleartype" content="on">
        <link rel="apple-touch-icon-precomposed" sizes="144x144" href="/back/img/apple-touch-icon-144x144-precomposed.png">

        <link rel="stylesheet" media="screen" href="/front/css/font-awesome.min.css">
        <link rel="stylesheet" media="screen" href="/back/css/style.css">
    </head>
    <body>
        <div class="breadcrumbs">
            <ul>
            <?php
                foreach($view->breadcrumbs as $bread)
                {
                    echo "<li><a title='".$bread['name']."' href='".$bread['reference']."'>".$bread['name']."</a></li>&nbsp;&sol;&nbsp;";
                }
            ?>
            </ul>
        </div>
        <?php 
            echo $this->layout()->content;
    
            if(!empty($view->cache->warning))
            {
                foreach ($view->cache->warning as $value)
                {
        ?>
              <div class="alert alert-warning">
                <button class="close" data-dismiss="alert"><i class="fa fa-times"></i></button>
                <i class="fa-fw fa fa-warning"></i> <?=$value?>
              </div>
        <?php
                }
                $view->cache->warning = null;
            }
            
            if(!empty($view->cache->success))
            {
                foreach ($view->cache->success as $value)
                {
        ?>
              <div class="alert alert-success">
                <button class="close" data-dismiss="alert"><i class="fa fa-times"></i></button>
                <i class="fa-fw fa fa-check"></i> <?=$value?>
              </div>
        <?php
                }
                $view->cache->success = null;
            }
              
            if(!empty($view->cache->error))
            {
                foreach ($view->cache->error as $value)
                {
        ?>
              <div class="alert alert-danger">
                <button class="close" data-dismiss="alert"><i class="fa fa-times"></i></button>
                <i class="fa-fw fa fa-times"></i> <?=$value?>
              </div>
        <?php
                }
                $view->cache->error = null;
            }
        ?>
        <script src="/front/js/jquery.min.js"></script>
        <script defer src="/back/js/custom.js"></script>
    </body>
</html>
